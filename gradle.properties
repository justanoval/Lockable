# Gradle Properties
org.gradle.jvmargs = -Xmx2G
org.gradle.parallel = true

# Mod Properties
version = 1.0.1
maven_group = io.github.justanoval
archives_base_name = lockable

# Publishing
# modrinth_id=...

# Dependencies are managed at gradle/libs.versions.toml
